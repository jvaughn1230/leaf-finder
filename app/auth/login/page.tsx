import React from "react";
import LoginForm from "@/components/LoginForm.client";

const page = () => {
  return (
    <div className="page">
      <LoginForm />
    </div>
  );
};

export default page;
